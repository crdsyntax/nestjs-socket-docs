{
  "name": "@crdsyntax/nestjs-socket-docs",
  "version": "0.1.1",
  "private": true,
  "description": "nestjs-socket-docs",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "publish:all": "pnpm -r publish --access public"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "typescript": "5.3.3"
  },
  "dependencies": {
    "changeset": "^0.2.6"
  }
}
