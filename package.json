{
  "name": "nestjs-socket-docs",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "postbuild": "cp -r packages/ui/dist/* packages/core/ui-dist/",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "version": "0.1.0"
}
